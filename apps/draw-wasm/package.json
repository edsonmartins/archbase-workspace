{
  "name": "@archbase/draw-wasm",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "postinstall": "node scripts/generate-stub.mjs",
    "dev": "npx serve public -p 3009 --cors",
    "build": "echo 'Pre-compiled WASM included in public/'",
    "typecheck": "echo 'WASM app - no TypeScript'"
  }
}
