{
  "name": "@archbase/workspace-core",
  "version": "0.1.0",
  "description": "Desktop shell and window manager for Archbase Workspace",
  "private": true,
  "scripts": {
    "dev": "rspack serve",
    "build": "rspack build",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "zustand": "^5.0.0",
    "@archbase/workspace-types": "workspace:*",
    "@archbase/workspace-state": "workspace:*"
  },
  "devDependencies": {
    "@rspack/core": "^1.2.0",
    "@rspack/cli": "^1.2.0",
    "@module-federation/enhanced": "^0.8.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
